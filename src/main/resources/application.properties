# Application Info
spring.application.name=contoscomusic

# Server Configuration
server.port=8100

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/contosco_music?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=OrangeGrape123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
# Or use "update" if you don't want to drop tables on restart
# spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger UI Configuration
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs